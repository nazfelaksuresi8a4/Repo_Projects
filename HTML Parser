import requests
from bs4 import BeautifulSoup

response = requests.get('https://news.ycombinator.com/')
soup = BeautifulSoup(response.text, 'html.parser')

links = []

# <a> etiketlerinden href değerlerini topla
for link in soup.find_all('a', href=True):
    href = link['href']
    links.append(href)

    # 20 linke ulaştıysak döngüyü durdur
    if len(links) == 20:
        break

# Linkleri yazdır
print("🟠 İlk 20 link:")
for i, link in enumerate(links, 1):
    print(f"{i}. {link}")
